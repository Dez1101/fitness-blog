import React from 'react';
import styled from 'styled-components';

const Header = () => {
    return (
      <Main>
      </Main>
    );
}

export default Header;

const Main = styled.header`
    background: url(../../images/weights.jpg) no-repeat center/cover;
    height: 25rem;
`;